<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="HealthMaui.Pages.PatientsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
       <VerticalStackLayout Spacing="10" Padding="12">
              <HorizontalStackLayout HorizontalOptions="FillAndExpand">
                     <Label Text="Patients" FontSize="24" VerticalOptions="Center" />
                     <Button Text="Add" HorizontalOptions="EndAndExpand" Clicked="AddClicked" />
              </HorizontalStackLayout>

              <CollectionView x:Name="PatientsList" SelectionMode="Single" SelectionChanged="PatientsList_SelectionChanged">
                     <CollectionView.ItemTemplate>
                            <DataTemplate>
                                   <SwipeView>
                                          <SwipeView.RightItems>
                                                 <SwipeItems Mode="Execute">
                                                        <SwipeItem Text="Delete" BackgroundColor="Red" Invoked="DeleteInvoked"/>
                                                 </SwipeItems>
                                          </SwipeView.RightItems>
                                          <Grid Padding="10">
                                                 <Label Text="{Binding Display}" />
                                          </Grid>
                                   </SwipeView>
                            </DataTemplate>
                     </CollectionView.ItemTemplate>
              </CollectionView>
       </VerticalStackLayout>
</ContentPage>
