<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="HealthMaui.Pages.PhysiciansPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
  <VerticalStackLayout Padding="16" Spacing="8">
    <Button Text="Add Physician" Clicked="AddClicked"/>
    <CollectionView x:Name="List" SelectionChanged="OnSelected">
      <CollectionView.ItemTemplate>
        <DataTemplate>
          <SwipeView>
            <SwipeView.RightItems>
              <SwipeItems>
                <SwipeItem Text="Delete" BackgroundColor="Tomato" CommandParameter="{Binding .}" Invoked="DeleteInvoked"/>
              </SwipeItems>
            </SwipeView.RightItems>
            <Grid Padding="8">
              <Label Text="{Binding Display}" />
            </Grid>
          </SwipeView>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>
  </VerticalStackLayout>
</ContentPage>
